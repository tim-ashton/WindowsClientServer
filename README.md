# WindowsClientServer


Basic windows client &amp; server sockets programs.

## Build
Open the sln with VS and build all.

## Run
Start the server program. It waits for a connection. Then start the client and it will connect then disconnect and exit.
**Add breakpoints to inspect running programs.**
